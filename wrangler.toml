# Cloudflare Pages 部署配置
# https://developers.cloudflare.com/pages/platform/build-configuration

# 构建命令
command = "npm run build"

# 构建输出目录
publish = "dist"

# Node.js 版本
[build.environment]
  NODE_VERSION = "20"

# SPA 路由重定向规则（_redirects 文件在 public 目录中）
